# Product Overview

This is a browser extension starter template built with WXT and React. The extension provides a foundation for developing cross-browser extensions with modern web technologies, supporting both Chrome and Firefox browsers.

The extension includes:

- A popup interface for user interaction
- Background scripts for extension logic
- Content scripts that can be injected into web pages
